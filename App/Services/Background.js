import { NativeModules } from 'react-native';

module.exports = {
    CategoryModule: NativeModules.CategoryModule,
    BookModule: NativeModules.BookModule,
    ChapterModule : NativeModules.ChapterModule,
    QuoteModule : NativeModules.QuoteModule
};